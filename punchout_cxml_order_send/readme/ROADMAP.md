- Optional **auto-send on PO confirm** (a per-backend flag) rather than the
  manual button.
- `ShipTo`/`BillTo`, tax and UoM blocks are a baseline; extend per the
  supplier's required fields / Extrinsics (cost center, requisition id, ...)
  once validated against their test account.
