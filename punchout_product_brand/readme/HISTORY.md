## 19.0.1.0.0 (2026-08)

- [ADD] First version. Adds a **brand** target to the punchout cart
  field-mapping framework, writing `product.brand` via the optional OCA
  `product_brand` module. Find-or-create by name (case-insensitive) with
  lookup-table support for supplier brand codes.
