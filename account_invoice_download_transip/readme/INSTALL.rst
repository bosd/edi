Before installing the module, you need to install the `transip python lib <https://github.com/roaldnefs/python-transip>`_ via the following command:

.. code::

  sudo pip3 install --upgrade python-transip
 